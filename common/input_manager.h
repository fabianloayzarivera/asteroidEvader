#pragma once
#include "sys.h"
#include "game.h"
#include "globals.h"

class InputManager {
private:
	//Player *playerPtr;
public:
	void processInput();
	//void setPlayerPtr(Player* p) { playerPtr = p; }
};